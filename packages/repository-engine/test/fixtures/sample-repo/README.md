# Sample App

This is a fixture repository used by @forge/repository-engine's test suite.
It exists purely to exercise the deterministic analyzers against a small,
realistic mix of a Next.js route, an Express server, Prisma, Stripe, and
next-auth so the tests aren't just asserting against mocks.
